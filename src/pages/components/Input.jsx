import React from "react";

const Input = () => {
    return (
        <div className="input">
            <input placeholder="enter a message"></input>
            <button>send</button>
        </div>
    );
};

export default Input;
